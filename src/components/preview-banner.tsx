export function PreviewBanner() {
  return (
    <div
      role="status"
      aria-live="polite"
      className="border-b border-brand/40 bg-brand/10 px-4 py-3 text-center text-sm font-semibold text-ink"
    >
      Modo de pré-visualização — está a ver o conteúdo como administradora. O seu progresso não é registado.
    </div>
  );
}
